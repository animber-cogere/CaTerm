#ifndef CATERM_WIDGET_WRAP_HPP
#define CATERM_WIDGET_WRAP_HPP

/// Text wrapping at end of line.
/** Wrap::Any  - Line is split at any character type.
 *  Wrap::Word - Line is split on work boundaries(space char). */
enum class Wrap { Any, Word };

#endif  // CATERM_WIDGET_WRAP_HPP
